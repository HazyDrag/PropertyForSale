@using PropertyForSale.Models
@model AdViewModel

@{
    ViewBag.Title = "Ad";
}
<div class="well" >
    <table>
        <tr>
            <td>
                <h1>@Model.Name</h1>
                @switch (Model.Status)
                {
                    case AdStatusModel.Active:
                        <h4>Status: <span class="label label-success">@Model.Status.ToString()</span></h4>
                        break;
                    case AdStatusModel.Pause:
                        <h4>Status: <span class="label label-warning">@Model.Status.ToString()</span></h4>
                        break;
                    case AdStatusModel.Stop:
                        <h4>Status: <span class="label label-danger">@Model.Status.ToString()</span></h4>
                        break;
                }
                <p>Type: @Model.Type</p>
                @if (Model.Photos.Count > 0)
                {
                    foreach (PhotoModel p in Model.Photos)
                    {
                        <img src="~/Images/Ad/@p.Path" width="500" alt="advert image">
                    }
                }
                else
                {
                <img src="~/Images/Ad/defaultAdImage.jpg" width="500" alt="advert image">
                }
                <p>@Model.Description</p>
                <h1>
                    <span class="label label-primary">@Model.Price.ToString("# $")</span>
                </h1>
            </td>
            <td>
                <blockquote>
                    <h3>Contacts:</h3>
                    <p>Name: @Model.UserName</p>
                    <p>Phone number: @Model.PhoneNumber</p>
                </blockquote>
            </td>
        </tr>
    </table>
</div>

